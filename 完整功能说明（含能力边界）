Econometrics AI Agent：完整功能说明（含能力边界）
1. 项目定位
Econometrics AI Agent 是一个面向计量实证研究的 AI 代理系统，核心是把研究问题自动转成“可执行分析流程”，并完成代码生成、执行、调试反思与多轮迭代。
它不是单纯聊天系统，而是“研究执行系统”。

2. 它能做什么（核心功能）
2.1 研究任务自动规划
将用户需求拆成结构化任务链（任务ID、依赖关系、任务类型、执行指令），并支持后续追加/修改计划。
适合金融实证从“问题”到“实证步骤”的落地。

2.2 自动生成并执行计量代码
使用 DataInterpreter 主链路，自动写代码并运行。
支持计量方法方向包括 IV-2SLS、DID、RDD、倾向得分方法等。

2.3 错误反思与自动修复
执行报错后可进入 reflection 机制：分析失败原因并生成改进实现，减少手工 debug 成本。

2.4 多轮连续研究
系统会判断新问题是否与历史问题相关，相关则复用同一解释器状态，适合“基准回归→稳健性→异质性”的连续迭代。

2.5 数据文件驱动实证
支持上传文件，后续请求会自动关联最近上传数据路径，便于直接在你的数据上跑分析。

2.6 实时输出执行过程
通过队列 + SSE 流式返回中间执行信息，便于追踪每一步做了什么。

2.7 网页信息检索能力（有限数据获取）
系统配置中有搜索工具和 URL 抓取工具开关，可用于网页信息补充。

3. 它在金融科研中主要覆盖哪些环节
研究执行设计：把题目拆解为可跑的计量流程。

数据到结果：清洗/建模/估计/输出结果。

结果迭代：稳健性、替代变量、样本窗口调整。

过程复现：保留执行轨迹，便于复盘与教学。
（对应能力见上文模块）

4. 能力边界（非常重要）
4.1 不是专门的数据平台连接器
不等同于现成对接 Wind/WRDS/CSMAR/Bloomberg 的标准化 ETL 平台。

可网页搜索与抓取，但不保证“自动找到高质量金融数据并规范化成可回归面板”的全流程稳定性。

4.2 不是“文献综述/创新点生成”专用系统
可辅助文字分析，但核心设计目标是计量执行，不是系统文献综述引擎或原创选题生成器。

4.3 不替代研究者的识别判断
工具可输出结果与解释草稿；

但因果识别有效性、经济机制可信度、学术贡献判定仍需研究者把关。

5. 最佳使用方式（推荐）
为了获得最稳结果，建议你给它：

明确研究问题

因变量/核心解释变量/控制变量

识别策略（DID/IV/RDD 等）

样本范围（市场、时间、频率）

数据文件（上传后可自动关联路径）

期望输出（回归表、图、稳健性、文字解释）

系统对“给定题目 + 给定数据 + 连续迭代实证”这类任务最强。


金融实战任务模板库（可直接喂给 Agent）
使用方法：
1）把每个模板中的 {} 换成你的内容；
2）数据先上传；
3）每次只跑一个主任务，结果出来后再迭代下一轮（稳健性/异质性/机制）。

模板 1：资产定价基准回归（时间序列 / 截面）
适用问题：某因子是否有定价能力？某特征是否预测收益？
输入要求：收益率、因子、控制变量、时间频率。

Prompt 模板（复制即可）

你是金融计量研究助理。请在我上传的数据上完成资产定价基准回归。

研究目标：
- 检验 {核心因子/特征} 对 {未来收益/超额收益} 的解释能力。

数据说明：
- 数据文件路径：{数据路径}
- 时间字段：{date_col}
- 资产标识：{asset_id_col}
- 因变量：{y_col}
- 核心自变量：{x_main_cols}
- 控制变量：{control_cols}
- 频率：{daily/weekly/monthly}

任务要求：
1) 数据清洗：缺失值、极端值处理（说明规则），并输出样本量变化。
2) 基准模型：先 OLS，再加入固定效应（如适用）。
3) 标准误：使用稳健标准误（并说明类型）。
4) 输出：回归表（系数、t值、p值、R²）、关键变量经济含义解释。
5) 额外：给出可复现代码和每一步中间结果摘要。

请先给出执行计划，再执行代码，再给结论。
模板 2：Fama-MacBeth 两步回归
请使用 Fama-MacBeth 方法检验 {特征变量} 的风险溢价显著性。

数据：
- 路径：{path}
- 月度收益：{ret_col}
- 资产ID：{asset_id}
- 日期：{date_col}
- 特征变量：{chars}
- 因子暴露（若已有）：{betas_cols}

要求：
1) 第一步（按时间截面）估计风险价格；
2) 第二步统计风险价格均值及t统计；
3) 对比是否加入控制变量时结果稳健；
4) 输出完整表格与结论：是否存在显著风险溢价。
模板 3：事件研究（Event Study）
请做事件研究，评估 {事件} 对 {股票收益/交易量/波动率} 的影响。

设定：
- 事件日字段：{event_date}
- 估计窗口：{[-120,-20]}
- 事件窗口：{[-5,+5]} 和 {[-10,+10]}
- 市场基准：{market_return_col}
- 因变量：{stock_return_col}

任务：
1) 构建正常收益模型（市场模型或你建议的替代模型）；
2) 计算 AR、CAR；
3) 对CAR做显著性检验；
4) 按行业/市值分组异质性；
5) 输出图（CAR曲线）+表+经济解释。
模板 4：DID（双重差分，政策评估）
请在我的数据上完成 DID 政策评估。

变量：
- 因变量：{y}
- treat组标识：{treat}
- 政策后期标识：{post}
- 交互项：treat*post
- 控制变量：{controls}
- 固定效应：{firm_fe, time_fe}

要求：
1) 基准DID回归；
2) 平行趋势检验（事件时间图/lead-lag）；
3) 稳健性：替代窗口、聚类标准误；
4) 安慰剂检验（伪政策时间或伪处理组）；
5) 输出政策效应大小、统计显著性、经济意义。
模板 5：三重差分（DDD）
请基于 DID 扩展为 DDD，新增度 {third_dimension}。

要求：
1) 构建 DDD 交互项并估计主效应；
2) 与 DID 结果并列表对比；
3) 解释第三维引入后识别含义变化；
4) 输出稳健性检查与图表。
模板 6：IV-2SLS（内生性处理）
请用 IV-2SLS 估计 {x} 对 {y} 的因果效应。

变量：
- 内生变量：{x_endog}
- 工具变量：{z_iv}
- 因变量：{y}
- 控制：{controls}

要求：
1) 第一阶段回归并报告工具强度（F统计）；
2) 第二阶段2SLS回归；
3) 必要诊断（弱工具/过度识别可行时给出）；
4) 与OLS结果比较并解释偏误方向；
5) 输出可发表风格表格和经济解释。
模板 7：RDD（断点回归）
请用 RDD 评估阈值政策效应。

设定：
- running variable：{running_var}
- cutoff：{c}
- outcome：{y}

要求：
1) 可视化断点附近关系；
2) 选择带宽并做局部线性估计；
3) 给出不同带宽稳健性；
4) 检查断点处操纵风险（可行时做密度检验）；
5) 输出断点效应、显著性和经济解释。
模板 8：PSM（倾向得分匹配）
请做 PSM 评估处理效应。

变量：
- 处理变量：{treat}
- 结果变量：{y}
- 协变量：{x_covs}

要求：
1) 估计倾向得分；
2) 最近邻/半径/核匹配（至少两种）；
3) 匹配前后平衡性检验；
4) 估计 ATT 并做稳健性；
5) 输出平衡性表、ATT表和解释。
模板 9：面板固定效应（Firm FE + Time FE）
请做企业-时间双向固定效应回归，研究 {x} 对 {y} 的影响。

要求：
1) 先 pooled OLS，再逐步加入 FE；
2) 报告聚类标准误（公司层或双向聚类）；
3) 检验多重共线性/异常值影响；
4) 输出主结果表和解释。
模板 10：波动率建模（GARCH 类）
请建模 {资产} 的波动率动态，比较 {GARCH, EGARCH, GJR-GARCH}。

要求：
1) 收益率预处理与平稳性检查；
2) 估计多种波动率模型并比较AIC/BIC；
3) 生成波动率预测并可视化；
4) 解释波动聚集、杠杆效应是否存在。
模板 11：VaR/ES 风险度量与回测
请基于 {资产收益序列} 估计 VaR 与 ES，并做回测。

要求：
1) 历史模拟 + 参数法（至少两种）；
2) 给出 95%/99% VaR 与 ES；
3) 回测（Kupiec/Christoffersen可行时）；
4) 输出风险结论与管理含义。
模板 12：组合构建与绩效归因
请根据 {排序变量} 构建分组组合并评估绩效。

要求：
1) 分组规则（如十分位）；
2) 计算多空组合收益；
3) 回归到常见因子模型做 alpha 检验；
4) 输出累计收益曲线、Sharpe、最大回撤；
5) 给出是否具备经济与统计显著性结论。
模板 13：因子“马赛克”稳健性任务包（一次性）
请把以下稳健性作为一个完整任务包执行：
- 替代因变量：{alt_y}
- 替代核心自变量：{alt_x}
- 替代样本窗口：{window_list}
- winsorize阈值：{winsor_rule}
- 不同标准误：{se_types}
- 分组异质性：{groups}

输出要求：
1) 主结果 + 稳健性总表（同口径）；
2) 每个稳健性项一句话解释“是否改变主结论”；
3) 给出最终结论稳定性评级（高/中/低）。
模板 14：机制检验（中介/渠道）
请检验 {x -> mechanism -> y} 机制是否成立。

要求：
1) 基准效应；
2) x 对 mechanism 的影响；
3) mechanism 加入后 x 系数变化；
4) 如可行，给出中介效应估计与显著性；
5) 输出机制是否成立、经济解释与局限。
模板 15：异质性检验（交互项+分组）
请做异质性检验，重点看 {group_var} 如何改变 {x 对 y} 的效应。

要求：
1) 交互项模型；
2) 分组回归；
3) 图形化对比边际效应；
4) 输出在哪类资产/企业中效应更强”。
模板 16：宏观-金融联动（VAR / 局部投影 LP）
请分析 {宏观冲击变量} 对 {金融变量} 的动态影响。

要求：
1) 先做平稳性与滞后阶选择；
2) VAR 或 LP 估计冲击响应；
3) 输出IRF图与置信区间；
4) 解释短期与中长期影响差异。
模板 17：预测任务（滚动窗口 + 严格防泄露）
请建立 {y} 的预测模型，严格防止未来信息泄露。

要求：
1) 时间序列切分（train/validation/test）；
2) 滚动或扩展窗口评估；
3) 比较基准模型与增强模型；
4) 指标：RMSE/MAE/R²_oos；
5) 输出预测性能与经济可用性结论。
模板 18：高频数据微观结构（若有高频数据）
请对高频交易数据做微观结构分析，关注 {价差/冲击成本/订单流}。

要求：
1) 数据清洗（异常时间戳、重复成交）；
2) 计算核心微观结构指标；
3) 事件前后或时段分层对比；
4) 输出交易摩擦与流动性含义。
模板 19：信用风险（违约/评级迁移）
请分析 {企业特征} 对 {违约概率/评级下调概率} 的影响。

要求：
1) 构建二元响应模型（Logit/Probit）；
2) 报告边际效应；
3) 样本不平衡处理（如需要）；
4) 输出模型判别能力与解释。
模板 20：论文结果自动化打包（最终交付）
请把当前研究输出整理成“论文结果包”。

要求：
1) 主表：基准结果；
2) 稳健性附表；
3) 异质性与机制表；
4) 图：核心趋势图/事件图/残差诊断图；
5) 文字：每张表对应的学术写作段落草稿（结果+经济解释+局限）。
一套“万能母模板”（你以后只改参数）
研究主题：{topic}
研究问题：{question}
识别策略：{DID/IV/RDD/FE/PSM/...}
数据路径：{path}
样本与频率：{sample_desc}
变量定义：
- 因变量：{y}
- 核心解释变量：{x}
- 控制变量：{controls}
- 分组变量（可选）：{group_vars}
- 工具变量/断点变量（可选）：{iv_or_running}

请按以下流程执行：
1) 任务规划（分步骤）
2) 数据清洗与描述统计
3) 基准模型估计
4) 关键识别检验（与策略匹配）
5) 稳健性检验（至少3项）
6) 异质性/机制检验（可行时）
7) 图表与结果总结
8) 给出可复现代码与结果解释

输出格式：
- 先给计划
- 再逐步执行
- 每一步输出中间结果
- 最后给“可写进论文”的总结段落
最后一句实话（避免踩坑）
你可以把它当“高级实证RA自动机”：
最强在“给定研究问题+给定数据后的执行与迭代”。
文献综述系统化、原创理论创新、专有数据库自动接入不是它主战场。
